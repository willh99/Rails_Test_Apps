<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>
  <ul class="users">
    <!-- infer @users is a list of User objects and interate using
         the partial _user -->
    <%= render @users %>
  </ul>
<%= will_paginate %>